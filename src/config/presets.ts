// Re-export bridge for tests — source in layers/editor/config/presets.ts
export {
  getSectionById,
  getSectionsByCategory,
  getTemplateById,
  getTemplatesByCategory,
  searchSections,
  searchTemplates,
  sectionCategories,
  sectionPresets,
  templateCategories,
  templatePresets,
} from '../../layers/editor/config/presets'
